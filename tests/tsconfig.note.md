Les tests utilisent l'exécution TypeScript native de Node 22
(`--experimental-strip-types`), sans dépendance supplémentaire.

    npm test

Les imports y portent l'extension `.ts` : Node l'exige, contrairement
au compilateur de Next.js.
